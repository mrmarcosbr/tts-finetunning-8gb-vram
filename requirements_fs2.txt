# Dependências para FastSpeech 2 com LoRA
# Use: python -m venv venv_fs2 && source venv_fs2/bin/activate && pip install -r requirements_fs2.txt

--extra-index-url https://download.pytorch.org/whl/cu129

torch==2.8.0+cu129
torchaudio==2.8.0+cu129
transformers<4.45.0
peft>=0.10.0
datasets<4.0.0
coqui-tts>=0.22.0
phonemizer
librosa
scipy
pyyaml
python-dotenv
soundfile
tqdm
speechbrain
