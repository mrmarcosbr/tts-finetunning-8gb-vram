# =============================================================================
# FastSpeech 2 / Coqui-TTS (transformers < 4.45 — incompatível com requirements.txt / HF 5.x).
#
# Ambiente SEPARADO do SpeechT5, por exemplo:
#   python -m venv venv_fs2
#   .\venv_fs2\Scripts\activate
#   python -m pip install -U pip
#   pip install -r requirements_fs2.txt
#
# NÃO misture com "pip install -r requirements.txt" neste mesmo venv (conflito de transformers).
#
# Nota: o comentário abaixo menciona uv; alternativa equivalente com pip é o bloco acima.
# =============================================================================

# Dependências para FastSpeech 2 com LoRA (instalação com uv, opcional):
# Use: uv venv venv_fs2 && uv pip install --python .\venv_fs2 --requirement requirements_fs2.txt --link-mode hardlink --index-strategy unsafe-best-match

--extra-index-url https://download.pytorch.org/whl/cu128

torch==2.8.0+cu128
torchaudio==2.8.0+cu128
transformers<4.45.0
peft>=0.10.0
datasets<4.0.0
coqui-tts>=0.22.0
phonemizer
librosa
scipy
pyyaml
python-dotenv
soundfile
tqdm
speechbrain
gruut-lang-pt
pydub
noisereduce==3.0.3
